{
  "name": "automotor-redesign",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/generate-seo-files.mjs && vite build && node scripts/prerender.mjs",
    "build:spa": "node scripts/generate-seo-files.mjs && vite build",
    "lint": "oxlint",
    "preview": "vite preview"
  },
  "devDependencies": {
    "oxlint": "^1.75.0",
    "playwright": "^1.62.1",
    "vite": "^8.2.0"
  }
}
